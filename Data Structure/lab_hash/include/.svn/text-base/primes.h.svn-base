/**
 * @file primes.h
 * Helper function for finding prime numbers.
 */

#ifndef CS225_PRIMES_H_
#define CS225_PRIMES_H_

#include <cstdint>

namespace cs225
{

/**
 * @param num The number to find the closest prime to in our list.
 * @return the closest prime greater than num that we we have in our list
 * of primes.
 */
uint64_t next_prime(uint64_t num);
}
#endif
